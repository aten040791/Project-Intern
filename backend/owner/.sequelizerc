var path = require("path");

module.exports = {
  config: path.resolve("configs", "config.json"),
  "migrations-path": path.resolve("database", "migrations"),
  "models-path": path.resolve("models"),
  "seeders-path": path.resolve("database", "seeders"),
};